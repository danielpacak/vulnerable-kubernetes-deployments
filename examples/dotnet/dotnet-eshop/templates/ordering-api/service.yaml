---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "ordering-api-service"
  labels:
    app.kubernetes.io/name: "eshop-apphost"
    app.kubernetes.io/component: "ordering-api"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
spec:
  type: "ClusterIP"
  selector:
    app.kubernetes.io/name: "eshop-apphost"
    app.kubernetes.io/component: "ordering-api"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
  ports:
    - name: "http"
      protocol: "TCP"
      port: {{ .Values.parameters.ordering_api.port_http | int }}
      targetPort: {{ .Values.parameters.ordering_api.port_http | int }}
