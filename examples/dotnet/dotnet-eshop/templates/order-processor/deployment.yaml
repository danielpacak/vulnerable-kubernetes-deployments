---
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "order-processor-deployment"
  labels:
    app.kubernetes.io/name: "eshop-apphost"
    app.kubernetes.io/component: "order-processor"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
spec:
  template:
    metadata:
      labels:
        app.kubernetes.io/name: "eshop-apphost"
        app.kubernetes.io/component: "order-processor"
        app.kubernetes.io/instance: "{{ .Release.Name }}"
    spec:
      containers:
        - image: "{{ .Values.parameters.order_processor.order_processor_image }}"
          name: "order-processor"
          envFrom:
            - configMapRef:
                name: "order-processor-config"
            - secretRef:
                name: "order-processor-secrets"
          ports:
            - name: "http"
              protocol: "TCP"
              containerPort: {{ .Values.parameters.order_processor.port_http | int }}
          imagePullPolicy: "IfNotPresent"
  selector:
    matchLabels:
      app.kubernetes.io/name: "eshop-apphost"
      app.kubernetes.io/component: "order-processor"
      app.kubernetes.io/instance: "{{ .Release.Name }}"
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: "RollingUpdate"
