[project]
name = "fastmcp2-weather-client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.135.1",
    "fastmcp>=3.1.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.6",
]
